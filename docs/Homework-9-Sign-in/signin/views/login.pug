doctype html
// login.html, a simple sign in web page, created by Yongye Fan, 18/12/2018
html(lang='en')
  head
    title Sakura 桜 | Sign in

    link(rel='stylesheet' type='text/css' href='/stylesheets/login.css' media='screen')
    link(rel='shortcut icon' type='image/png' href='/images/flower.png')

    script(src='https://cdn.bootcss.com/jquery/3.3.1/jquery.min.js')
    script(src='/javascripts/validityCheck.js')
    script(src='/javascripts/login.js')

  body
    .container
      .logo
        img(src='/images/flower.png' alt='Sakura')

      if ifAllow
        .info-bar Sakura 桜
      else
        .info-bar.access-error 请先登录

      form.input-part(method='POST')
        .username
          img(src='/images/username.png' alt='用户名' title='用户名')

          if !error
            input(name='username' value='用户名')
            .error
          else
            input(name='username' value=error.username.content)
            .error.error-show #{error.username.message}

        .password
          img(src='/images/password.png' alt='密码' title='密码')
          input(name='password' value='密码')

          if !error
            .error
          else
            .error.error-show #{error.password.message}

        .buttons
          input.submit(type='submit' value='登录')
          .signup
            a.signup-button(href='/regist' title='注册账号') 注册
            .signup-note 没有账号?
